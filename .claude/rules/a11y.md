---
description: 접근성(a11y) 규칙
paths: ["src/**/*.tsx"]
---

# 접근성 컨벤션

- 모달: `aria-labelledby` (`useId()` + Context)
- 트리거: `aria-expanded`
- 로딩 버튼: `aria-busy`
- 아이콘 버튼: `aria-label`
- 장식 아이콘: `aria-hidden="true"`
